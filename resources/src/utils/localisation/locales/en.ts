const pages = {
  'pages.home': 'Home',
  'pages.history': 'History',
};

const components = {
  'components.fileLoader.load_a_file': 'Upload a file',
};

const system = {
  'system.send': 'Envoyer',
};

export default {
  ...components,
  ...pages,
  ...system,
};
